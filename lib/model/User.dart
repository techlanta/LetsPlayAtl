class User {
  String name;
  String email;
  String password;

  User(this.name, this.email, this.password);

  bool login(email, password) {
    bool status;
    //check to see if login works
    return status;
  }

}